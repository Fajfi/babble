<div class="fullpage-chat">
  {{#if topic.id}}
    <div class="chat-pane">
      {{mount-widget widget="babble-chat" args=(as-hash topic=topic hideTitle=hideTitle)}}
    </div>
  {{else}}
    {{loading-spinner}}
  {{/if}}
</div>
